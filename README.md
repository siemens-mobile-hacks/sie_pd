# SiePD – ELF editor for Siemens mobile phones

SiePD is an ELF application for Siemens mobile phones that allows editing `.pd` configuration files directly on the device.

## Building
The SDK must be located at `../../sdk` relative to the project root.
```bash
mkdir build && cd build
cmake ..
make
```
